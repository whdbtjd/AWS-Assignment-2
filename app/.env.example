# Local dev (npm start) — copy to .env and fill in
PORT=3001
DB_HOST=localhost
DB_NAME=acmedb
DB_USER=acmeadmin
DB_PASSWORD=your-password-here

# EC2 / production — set by user_data (do not commit .env)
# DB_SECRET_ARN=arn:aws:secretsmanager:...
# AWS_REGION=ap-northeast-2
